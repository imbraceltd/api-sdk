---
title: 快速入門
description: 在短短一分鐘內發出你的第一個 Imbrace API 呼叫。
---

import { Tabs, TabItem } from "@astrojs/starlight/components";
import AuthOnly from "../../../../components/AuthOnly.astro";

### 1. 初始化客戶端

<AuthOnly mode="access-token">
<Tabs syncKey="lang">
  <TabItem label="TypeScript">
    ```typescript
    import { ImbraceClient } from "@imbrace/sdk"

    const client = new ImbraceClient({
      accessToken: "your-access-token",
      baseUrl: "https://app-gatewayv2.imbrace.co",
    })
    ```
  </TabItem>
  <TabItem label="Python">
    ```python
    import os
    from dotenv import load_dotenv
    from imbrace import ImbraceClient

    load_dotenv()

    client = ImbraceClient(
        access_token=os.environ["IMBRACE_ACCESS_TOKEN"],
        base_url="https://app-gatewayv2.imbrace.co",
    )
    ```
  </TabItem>
</Tabs>
</AuthOnly>

<AuthOnly mode="api-key">
<Tabs syncKey="lang">
  <TabItem label="TypeScript">
    ```typescript
    import { ImbraceClient } from "@imbrace/sdk"

    const client = new ImbraceClient({
      apiKey: "your-api-key",
      baseUrl: "https://app-gatewayv2.imbrace.co",
    })
    ```
  </TabItem>
  <TabItem label="Python">
    ```python
    import os
    from dotenv import load_dotenv
    from imbrace import ImbraceClient

    load_dotenv()

    client = ImbraceClient(
        api_key=os.environ["IMBRACE_API_KEY"],
        base_url="https://app-gatewayv2.imbrace.co",
    )
    ```
  </TabItem>
</Tabs>
</AuthOnly>

> 切換標題中的**驗證**下拉選單，可在全站範圍內切換 API Key 與 Access Token 範例。請參閱[驗證](/sdk/authentication/)以決定選擇哪一種。

---

### 2. 取得你的看板

看板即 CRM 管道 — 潛在客戶、交易、任務或任何結構化資料。此呼叫只需要你的憑證。

<Tabs syncKey="lang">
  <TabItem label="TypeScript">
    ```typescript
    const { data: boards } = await client.boards.list()

    for (const board of boards) {
      console.log(board._id, board.doc_name)
    }
    ```
  </TabItem>
  <TabItem label="Python">
    ```python
    boards = client.boards.list()

    for board in boards.get("data", []):
        print(board["_id"], board.get("doc_name"))
    ```
  </TabItem>
</Tabs>

如果你看到你的看板列表，表示 SDK 已連線並通過驗證。

---

### 3. 下一步

[完整流程指南](/sdk/full-flow-guide/)逐步介紹四大主要工作流程。直接跳到某個章節：

- **AI Agent + streamChat** → [完整流程指南 §1](/sdk/full-flow-guide/#1-create-an-ai-agent-and-start-chatting)
- **工作流程** → [完整流程指南 §2](/sdk/full-flow-guide/#2-create-a-workflow-and-bind-it-to-an-ai-agent)
- **知識庫（資料夾、RAG）** → [完整流程指南 §3](/sdk/full-flow-guide/#3-manage-knowledge-hubs-and-attach-to-an-ai-agent)
- **看板與項目 (CRM)** → [完整流程指南 §4](/sdk/full-flow-guide/#4-manage-data-boards-and-items-crm-pipelines)

關於每個命名空間的 API 參考，請參閱[資源](/sdk/resources/)。
