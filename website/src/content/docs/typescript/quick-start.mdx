---
title: TypeScript SDK — Quick Start
description: Make your first Imbrace API call in under a minute.
---

### 1. Initialize the Client

```typescript
import { ImbraceClient } from "@imbrace/sdk"

const client = new ImbraceClient({
  accessToken: "your-access-token",
  baseUrl: "https://app-gatewayv2.imbrace.co",
})
```

> Pass `apiKey` instead if Imbrace is a feature inside your own backend (vs. powering it). See [Authentication](/typescript/authentication/) for the decision.

---

### 2. Fetch Your Boards

Boards are CRM pipelines — leads, deals, tasks, or any structured data. This call requires only your access token.

```typescript
const { data: boards } = await client.boards.list()

for (const board of boards) {
  console.log(board._id, board.doc_name)
}
```

That's it. If you see your boards listed, the SDK is connected and authenticated.

---

### 3. Next Steps

The [Full Flow Guide](/typescript/full-flow-guide/) walks the four major workflows end-to-end. Jump straight to a section:

- **AI Assistant + streamChat** → [Full Flow Guide §1](/typescript/full-flow-guide/#1-create-an-ai-assistant-and-start-chatting)
- **Activepieces workflows** → [Full Flow Guide §2](/typescript/full-flow-guide/#2-create-a-workflow-with-activepieces-and-bind-it-to-an-assistant)
- **Knowledge Hub (folders, RAG)** → [Full Flow Guide §3](/typescript/full-flow-guide/#3-manage-knowledge-hubs-and-attach-to-an-assistant)
- **Boards & Items (CRM)** → [Full Flow Guide §4](/typescript/full-flow-guide/#4-manage-data-boards-and-items-crm-pipelines)

For the full per-namespace API reference, see [Resources](/typescript/resources/).

