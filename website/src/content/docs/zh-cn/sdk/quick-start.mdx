---
title: 快速入门
description: 在 60 秒内发起你的首次 Imbrace API 调用。
---

import { Tabs, TabItem } from "@astrojs/starlight/components";
import AuthOnly from "../../../../components/AuthOnly.astro";

### 1. 初始化客户端

<AuthOnly mode="access-token">
<Tabs syncKey="lang">
  <TabItem label="TypeScript">
    ```typescript
    import { ImbraceClient } from "@imbrace/sdk"

    const client = new ImbraceClient({
      accessToken: "your-access-token",
      baseUrl: "https://app-gatewayv2.imbrace.co",
    })
    ```
  </TabItem>
  <TabItem label="Python">
    ```python
    import os
    from dotenv import load_dotenv
    from imbrace import ImbraceClient

    load_dotenv()

    client = ImbraceClient(
        access_token=os.environ["IMBRACE_ACCESS_TOKEN"],
        base_url="https://app-gatewayv2.imbrace.co",
    )
    ```
  </TabItem>
</Tabs>
</AuthOnly>

<AuthOnly mode="api-key">
<Tabs syncKey="lang">
  <TabItem label="TypeScript">
    ```typescript
    import { ImbraceClient } from "@imbrace/sdk"

    const client = new ImbraceClient({
      apiKey: "your-api-key",
      baseUrl: "https://app-gatewayv2.imbrace.co",
    })
    ```
  </TabItem>
  <TabItem label="Python">
    ```python
    import os
    from dotenv import load_dotenv
    from imbrace import ImbraceClient

    load_dotenv()

    client = ImbraceClient(
        api_key=os.environ["IMBRACE_API_KEY"],
        base_url="https://app-gatewayv2.imbrace.co",
    )
    ```
  </TabItem>
</Tabs>
</AuthOnly>

> 切换顶部的**Auth**下拉菜单可在全站 API Key 和 Access Token 示例之间切换。关于如何选择，参见[认证](/sdk/authentication/)。

---

### 2. 获取你的看板

看板是 CRM 管道——线索、交易、任务或任何结构化数据。此调用仅需你的凭证。

<Tabs syncKey="lang">
  <TabItem label="TypeScript">
    ```typescript
    const { data: boards } = await client.boards.list()

    for (const board of boards) {
      console.log(board._id, board.doc_name)
    }
    ```
  </TabItem>
  <TabItem label="Python">
    ```python
    boards = client.boards.list()

    for board in boards.get("data", []):
        print(board["_id"], board.get("doc_name"))
    ```
  </TabItem>
</Tabs>

如果你能看到你的看板列表，则 SDK 已成功连接并通过认证。

---

### 3. 下一步

[完整流程指南](/sdk/full-flow-guide/)端到端地介绍了四大工作流。直接跳转到相关章节：

- **AI 代理 + streamChat** → [完整流程指南 §1](/sdk/full-flow-guide/#1-create-an-ai-agent-and-start-chatting)
- **工作流** → [完整流程指南 §2](/sdk/full-flow-guide/#2-create-a-workflow-and-bind-it-to-an-ai-agent)
- **知识库（文件夹、RAG）** → [完整流程指南 §3](/sdk/full-flow-guide/#3-manage-knowledge-hubs-and-attach-to-an-ai-agent)
- **看板与条目（CRM）** → [完整流程指南 §4](/sdk/full-flow-guide/#4-manage-data-boards-and-items-crm-pipelines)

按命名空间的 API 参考，参见[资源](/sdk/resources/)。
