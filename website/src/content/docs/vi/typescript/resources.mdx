---
title: TypeScript SDK — Tính Năng
description: Hướng dẫn xây dựng CRM và AI với Imbrace TypeScript SDK.
---

```typescript
import { ImbraceClient } from "@imbrace/sdk"
const client = new ImbraceClient({ accessToken: "acc_xxx" })
```

---

## CRM

### Quản Lý Contacts

```typescript
// Danh sách contacts
const { data: contacts } = await client.contacts.list({ limit: 50 })

// Lấy một contact
const contact = await client.contacts.get("contact_id")

// Cập nhật thông tin
await client.contacts.update("contact_id", {
  name: "Alice B.",
  email: "alice@example.com",
  phone: "+84901234567",
})

// Lịch sử hoạt động, bình luận, file
const activity = await client.contacts.getActivity("contact_id")
const comments  = await client.contacts.getComments("contact_id")
const files     = await client.contacts.getFiles("contact_id")
```

---

### CRM Boards (Pipeline)

Boards là nơi lưu trữ dữ liệu CRM — leads, deals, tasks, hoặc bất kỳ dữ liệu có cấu trúc nào.

```typescript
// Danh sách boards
const { data: boards } = await client.boards.list()

// Lấy items của một board (ví dụ: pipeline leads)
const { data: leads } = await client.boards.listItems("board_id", { limit: 100 })

// Tạo item mới (ví dụ: lead mới)
const lead = await client.boards.createItem("board_id", {
  fields: { name: "Acme Corp", status: "new", value: 50000 },
})

// Cập nhật item (ví dụ: chuyển sang giai đoạn tiếp theo)
await client.boards.updateItem("board_id", lead.id, {
  fields: { status: "qualified" },
})

// Tìm kiếm items
const results = await client.boards.search("board_id", { query: "enterprise" })

// Xuất CSV
const csv = await client.boards.exportCsv("board_id")
```

---

### Conversations

Quản lý cuộc hội thoại với khách hàng — xem, phân công, cập nhật trạng thái.

```typescript
// Tìm kiếm conversations
const { data: convs } = await client.conversations.search({
  businessUnitId: "bu_xxx",
  q: "support",
  limit: 20,
})

// Conversations chưa xử lý
const { data: open } = await client.conversations.getOutstanding({
  businessUnitId: "bu_xxx",
  limit: 50,
})

// Phân công cho thành viên
await client.conversations.assignTeamMember({
  conversation_id: "conv_xxx",
  user_id: "user_xxx",
})

// Cập nhật trạng thái
await client.conversations.updateStatus({
  conversation_id: "conv_xxx",
  status: "resolved",
})
```

---

### Nhắn Tin

Gửi và nhận tin nhắn qua các kênh (web, WhatsApp, Facebook, email, ...).

```typescript
// Danh sách kênh
const channels = await client.channel.list()

// Gửi tin nhắn
await client.messages.send("conversation_id", {
  parts: [{ type: "text", text: "Xin chào, tôi có thể giúp gì cho bạn?" }],
})

// Danh sách tin nhắn
const msgs = await client.messages.list("conversation_id")

// Đánh dấu đã đọc
await client.channel.markRead("conversation_id")
```

---

### Workflows & Automation

Kích hoạt channel automations để xử lý tin nhắn đến, phân luồng hội thoại và kích hoạt các hành động follow-up.

```typescript
// Danh sách tất cả channel automations
const { data: automations } = await client.workflows.listChannelAutomation()

// Lọc theo loại kênh
const whatsappFlows = await client.workflows.listChannelAutomation({
  channelType: "whatsapp",
})
```

---

## AI

### AI Service (aiv2)

`client.ai` và `client.chatAi` đều gọi đến dịch vụ **aiv2**. Dùng `client.ai` cho các lệnh gọi OpenAI-compatible; dùng `client.chatAi` cho phiên chat và xử lý tài liệu.

```typescript
// ── client.ai — OpenAI-compatible completions ──────────────────────────────

const response = await client.ai.complete({
  model: "gpt-4o",
  messages: [
    { role: "system", content: "Bạn là trợ lý CRM." },
    { role: "user",   content: "Tóm tắt ghi chú khách hàng này: ..." },
  ],
})
console.log(response.choices[0].message.content)

for await (const chunk of client.ai.stream({
  model: "gpt-4o",
  messages: [{ role: "user", content: "Soạn email follow-up cho lead này." }],
})) {
  process.stdout.write(chunk.choices[0]?.delta?.content ?? "")
}

const embeddings = await client.ai.embed({
  model: "text-embedding-ada-002",
  input: ["khách hàng khiếu nại về billing", "vấn đề billing leo thang"],
})

// ── client.chatAi — managed chat sessions ─────────────────────────────────

const models   = await client.chatAi.listModels()
const response2 = await client.chatAi.chat({
  model: "gpt-4o",
  messages: [{ role: "user", content: "Chính sách hoàn tiền là gì?" }],
})
const chat    = await client.chatAi.createChat({ title: "Support Chat" })
const history = await client.chatAi.listChats()
await client.chatAi.deleteChat(chat.id)

// ── Hiểu Tài Liệu ─────────────────────────────────────────────────────────

const result = await client.chatAi.processDocument({
  modelName: "gpt-4o",
  url: "https://example.com/invoice.pdf",
  organizationId: "org_xxx",  // bắt buộc cho endpoint này
})
console.log(result.data)
// { invoice_number: "INV-001", total: 1200, vendor: "Acme Corp", ... }
```

---

### AI Agent

Chat streaming với knowledge base (embeddings) và quản lý phiên chat end-user.

```typescript
// Stream chat với AI assistant (SSE)
// id là session/conversation ID — truyền cùng id qua các lượt để giữ lịch sử hội thoại.
//   Chỉ bỏ qua khi bắt đầu cuộc trò chuyện mới; SDK sẽ tự tạo nếu không truyền.
// user_id là tuỳ chọn — được lấy tự động qua chat-client/auth/user khi không cung cấp.
const sessionId = crypto.randomUUID() // lưu lại id này cho toàn bộ cuộc trò chuyện

const response = await client.aiAgent.streamChat({
  id: sessionId,
  assistant_id: "assistant_id",
  organization_id: "org_xxx",
  messages: [{ role: "user", content: "Deals nào đã chốt quý này?" }],
})

// Gợi ý câu hỏi
const suggestions = await client.aiAgent.getAgentPromptSuggestion("assistant_id")

// Knowledge base
const files      = await client.aiAgent.listEmbeddingFiles()
const classified = await client.aiAgent.classifyFile({
  url: "https://example.com/product-catalog.pdf",
  mime_type: "application/pdf",
})

// Quản lý phiên chat end-user
const chat = await client.aiAgent.createClientChat({
  id: crypto.randomUUID(),
  assistantId: "assistant_id",
  organizationId: "org_xxx",
  userId: "user_id",               // lấy từ chat-client/auth/user
  selectedVisibilityType: "private",
  message: {
    id: crypto.randomUUID(),
    role: "user",
    content: "Xin chào",
    createdAt: new Date().toISOString(),
    parts: [{ type: "text", text: "Xin chào" }],
  },
})

const messages = await client.aiAgent.listClientMessages(chat.id)
await client.aiAgent.deleteClientChat(chat.id)
```
