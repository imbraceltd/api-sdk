---
title: Python SDK — Xác Thực
description: API Key và Access Token trong Imbrace Python SDK.
---

SDK hỗ trợ hai chiến lược xác thực. Truyền `api_key` hoặc `access_token` vào constructor — transport tự xử lý headers.

### Các Chiến Lược Xác Thực

| Chiến lược       | Khi nào dùng                                | Header gửi đi             |
| ---------------- | ------------------------------------------- | ------------------------- |
| **API Key**      | Backend scripts, Celery tasks, server calls | `x-api-key: sk-xxx...`    |
| **Access Token** | Sau khi user đăng nhập (OTP hoặc password)  | `x-access-token: acc_...` |

---

### API Key (Server-Side)

```python
import os
from imbrace import ImbraceClient

client = ImbraceClient(
    api_key=os.environ["IMBRACE_API_KEY"],
)
```

---

### Access Token (Client-Side)

Dùng sau khi user xác thực:

```python
client = ImbraceClient(
    access_token="acc_xxxxxxxxxxxxx",
)
```

---

### Luồng OTP Login

Cách được khuyến nghị cho user-facing apps — SDK tự lưu token sau khi đăng nhập thành công.

```python
from imbrace import ImbraceClient

client = ImbraceClient()

# Bước 1: Gửi OTP đến email người dùng
client.request_otp("user@example.com")

# Bước 2: Người dùng nhập OTP — token được lưu tự động
client.login_with_otp("user@example.com", "123456")

# Bước 3: Tất cả các call tiếp theo đã được xác thực
me = client.platform.get_me()
```

Đăng nhập bằng email + password cũng được hỗ trợ:

```python
client.login("user@example.com", "password123")
```

#### Async OTP Flow

```python
from imbrace import AsyncImbraceClient

async with AsyncImbraceClient() as client:
    await client.request_otp("user@example.com")
    await client.login_with_otp("user@example.com", "123456")
    me = await client.platform.get_me()
```

---

### Quản Lý Token

```python
# Thay token (ví dụ: sau khi OAuth refresh)
client.set_access_token("new_token...")

# Xóa token (ví dụ: khi logout)
client.clear_access_token()
```

---

### Context Manager (Khuyến Nghị)

```python
# Synchronous
with ImbraceClient(api_key="sk-xxx") as client:
    me = client.platform.get_me()

# Asynchronous
async with AsyncImbraceClient(api_key="sk-xxx") as client:
    me = await client.platform.get_me()
```

---

### Thứ Tự Ưu Tiên Cấu Hình

1. **Constructor params tường minh** — `ImbraceClient(api_key="...")`
2. **Environment variables** — `IMBRACE_API_KEY`, `IMBRACE_GATEWAY_URL`
3. **Defaults** — `env="stable"`, gateway `https://app-gatewayv2.imbrace.co`
