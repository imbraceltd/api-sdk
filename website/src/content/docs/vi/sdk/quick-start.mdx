---
title: Bắt đầu nhanh
description: Thực hiện lệnh gọi API Imbrace đầu tiên trong vòng chưa đầy một phút.
---

import { Tabs, TabItem } from "@astrojs/starlight/components";
import AuthOnly from "../../../../components/AuthOnly.astro";

### 1. Khởi tạo client

<AuthOnly mode="access-token">
<Tabs syncKey="lang">
  <TabItem label="TypeScript">
    ```typescript
    import { ImbraceClient } from "@imbrace/sdk"

    const client = new ImbraceClient({
      accessToken: "your-access-token",
      baseUrl: "https://app-gatewayv2.imbrace.co",
    })
    ```
  </TabItem>
  <TabItem label="Python">
    ```python
    import os
    from dotenv import load_dotenv
    from imbrace import ImbraceClient

    load_dotenv()

    client = ImbraceClient(
        access_token=os.environ["IMBRACE_ACCESS_TOKEN"],
        base_url="https://app-gatewayv2.imbrace.co",
    )
    ```
  </TabItem>
</Tabs>
</AuthOnly>

<AuthOnly mode="api-key">
<Tabs syncKey="lang">
  <TabItem label="TypeScript">
    ```typescript
    import { ImbraceClient } from "@imbrace/sdk"

    const client = new ImbraceClient({
      apiKey: "your-api-key",
      baseUrl: "https://app-gatewayv2.imbrace.co",
    })
    ```
  </TabItem>
  <TabItem label="Python">
    ```python
    import os
    from dotenv import load_dotenv
    from imbrace import ImbraceClient

    load_dotenv()

    client = ImbraceClient(
        api_key=os.environ["IMBRACE_API_KEY"],
        base_url="https://app-gatewayv2.imbrace.co",
    )
    ```
  </TabItem>
</Tabs>
</AuthOnly>

> Chuyển đổi **Auth** dropdown trong tiêu đề để chuyển qua lại giữa các ví dụ API Key và Access Token trên toàn bộ trang web. Xem [Xác thực](/sdk/authentication/) để biết nên chọn loại nào.

---

### 2. Lấy danh sách boards của bạn

Boards là các pipeline CRM — khách hàng tiềm năng, thỏa thuận, nhiệm vụ hoặc bất kỳ dữ liệu có cấu trúc nào. Lệnh gọi này chỉ yêu cầu thông tin xác thực của bạn.

<Tabs syncKey="lang">
  <TabItem label="TypeScript">
    ```typescript
    const { data: boards } = await client.boards.list()

    for (const board of boards) {
      console.log(board._id, board.doc_name)
    }
    ```
  </TabItem>
  <TabItem label="Python">
    ```python
    boards = client.boards.list()

    for board in boards.get("data", []):
        print(board["_id"], board.get("doc_name"))
    ```
  </TabItem>
</Tabs>

Nếu bạn thấy danh sách boards của mình, SDK đã được kết nối và xác thực.

---

### 3. Các bước tiếp theo

[Hướng dẫn toàn luồng](/sdk/full-flow-guide/) hướng dẫn bốn quy trình chính từ đầu đến cuối. Chuyển thẳng đến một phần:

- **AI Agent + streamChat** → [Hướng dẫn toàn luồng §1](/sdk/full-flow-guide/#1-create-an-ai-agent-and-start-chatting)
- **Workflows** → [Hướng dẫn toàn luồng §2](/sdk/full-flow-guide/#2-create-a-workflow-and-bind-it-to-an-ai-agent)
- **Knowledge Hub (thư mục, RAG)** → [Hướng dẫn toàn luồng §3](/sdk/full-flow-guide/#3-manage-knowledge-hubs-and-attach-to-an-ai-agent)
- **Boards & Items (CRM)** → [Hướng dẫn toàn luồng §4](/sdk/full-flow-guide/#4-manage-data-boards-and-items-crm-pipelines)

Để tham khảo API theo từng không gian tên, xem [Tài nguyên](/sdk/resources/).
